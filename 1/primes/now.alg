start 
input n                        # a 
i = 2                          # b

loop:
  r = n % i                    # c
  if r == 0 : print "number is not prime " and exit the program # dj
  i = i + 1 # e
if i <= sqrt(n) : goto loop # 100f 

print number is prime # g

end 

time = a +b + n*(c + d +e+ f) + g


